# @generated by autocargo from //common/rust/shed/tokio-detectors:tokio-detectors

[package]
name = "tokio-detectors"
version = "0.1.0"
authors = ["Facebook <opensource+rust-shed@fb.com>"]
edition = "2021"
description = "Provides utility for detection of blocked tokio runtimes."
readme = "../../README.md"
repository = "https://github.com/facebookexperimental/rust-shed"
license = "MIT OR Apache-2.0"

[dependencies]
rand = { version = "0.8", features = ["small_rng"] }
tokio = { version = "1.37.0", features = ["full", "test-util", "tracing"] }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2.139"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2.139"
