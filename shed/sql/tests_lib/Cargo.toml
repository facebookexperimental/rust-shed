# @generated by autocargo from //common/rust/shed/sql:sql_tests_lib

[package]
name = "sql_tests_lib"
version = "0.1.0"
authors = ["Facebook <opensource+rust-shed@fb.com>"]
edition = "2024"
description = "Test library for sql crate"
readme = "../../../README.md"
repository = "https://github.com/facebookexperimental/rust-shed"
license = "MIT OR Apache-2.0"

[lib]
path = "lib.rs"

[dependencies]
anyhow = "1.0.98"
chrono = { version = "0.4.41", features = ["clock", "serde", "std"], default-features = false }
fbinit = { version = "0.2.0", path = "../../fbinit" }
rand = { version = "0.8", features = ["small_rng"] }
sql = { version = "0.1.0", path = ".." }

[lints]
rust = { unexpected_cfgs = { check-cfg = ["cfg(fbcode_build)"], level = "warn" } }
