# @generated by autocargo from //common/rust/shed/slog_glog_fmt:[slog_glog_fmt,slog_glog_fmt_example]

[package]
name = "slog_glog_fmt"
version = "0.1.0"
authors = ["Facebook <opensource+rust-shed@fb.com>"]
edition = "2024"
description = "slog Drain for glog-formatted logs"
readme = "../../README.md"
repository = "https://github.com/facebookexperimental/rust-shed"
license = "MIT OR Apache-2.0"

[[bin]]
name = "slog_glog_fmt_example"
path = "example/main.rs"

[dependencies]
anyhow = "1.0.98"
chrono = { version = "0.4.41", features = ["clock", "serde", "std"], default-features = false }
failure_ext = { version = "0.1.0", path = "../failure_ext" }
hostname = "0.3"
itertools = "0.14.0"
slog = { version = "2.8.2", features = ["max_level_trace", "nested-values"] }
slog-term = "2.9.2"

[dev-dependencies]
rand = { version = "0.8", features = ["small_rng"] }
regex = "1.12.2"
thiserror = "2.0.12"

[target.'cfg(target_os = "linux")'.dependencies]
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }

[target.'cfg(target_os = "macos")'.dependencies]
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }
