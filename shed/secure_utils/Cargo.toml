# @generated by autocargo from //common/rust/shed/secure_utils:secure_utils

[package]
name = "secure_utils"
version = "0.1.0"
authors = ["Facebook <opensource+rust-shed@fb.com>"]
edition = "2024"
description = "Crate with useful security utilities"
readme = "../../README.md"
repository = "https://github.com/facebookexperimental/rust-shed"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1.0.98"
hex = { version = "0.4.3", features = ["alloc"] }
libc = "0.2.139"
openssl = "0.10.72"
openssl-sys = "0.9.99"
serde = { version = "1.0.219", features = ["derive", "rc"] }
serde_json = { version = "1.0.140", features = ["alloc", "float_roundtrip", "unbounded_depth"] }
slog = { version = "2.7", features = ["max_level_trace", "nested-values"] }
tracing_slog_compat = { version = "0.1.0", path = "../tracing_slog_compat" }

[lints]
rust = { unexpected_cfgs = { check-cfg = ["cfg(fbcode_build)"], level = "warn" } }
