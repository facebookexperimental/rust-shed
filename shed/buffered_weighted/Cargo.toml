# @generated by autocargo from //common/rust/shed/buffered_weighted:buffered_weighted

[package]
name = "buffered_weighted"
version = "0.1.0"
authors = ["Facebook <opensource+rust-shed@fb.com>"]
edition = "2021"
description = "Ordered stream implementation that allows for concurrent polling constrained by the weight of the futures"
readme = "../../README.md"
repository = "https://github.com/facebookexperimental/rust-shed"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1.0.95"
futures-util = { version = "0.3.30", features = ["compat"] }
pin-project = "0.4.30"

[dev-dependencies]
futures = { version = "0.3.30", features = ["async-await", "compat"] }
proptest = "1.5"
proptest-derive = "0.5"
tokio = { version = "1.44.2", features = ["full", "test-util", "tracing"] }
tokio-stream = { version = "0.1.16", features = ["fs", "io-util", "net", "signal", "sync", "time"] }

[target.'cfg(target_os = "linux")'.dependencies]
procfs = "0.15.1"
